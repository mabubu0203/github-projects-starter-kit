name: "⑤ 統合プロジェクト分析"

on:
  workflow_dispatch:
    inputs:
      project_number:
        description: "Project の Number"
        required: true
        type: number
      report_types:
        description: "実行するレポート/機能タイプ（all: 全機能, stale: 滞留検知, summary: サマリー, effort: 工数集計, velocity: ベロシティ, export: アイテムエクスポート）"
        required: true
        type: choice
        default: "all"
        options:
          - all
          - stale
          - summary
          - effort
          - velocity
          - export
      output_format:
        description: "出力形式"
        required: true
        type: choice
        default: "json"
        options:
          - json
          - markdown
          - csv
          - tsv
      item_type:
        description: "対象アイテムの種別"
        required: true
        type: choice
        default: "all"
        options:
          - all
          - issues
          - prs
      item_state:
        description: "対象アイテムの状態（open / closed / all）"
        required: true
        type: choice
        default: "all"
        options:
          - all
          - open
          - closed
      retention_days:
        description: "アーティファクトの保持日数（1〜7）"
        required: true
        type: number
        default: 7

permissions:
  contents: read

jobs:
  validate-inputs:
    runs-on: ubuntu-latest
    steps:
      - name: retention_days の範囲チェック（1〜7）
        run: |
          if [ "${{ inputs.retention_days }}" -lt 1 ] || [ "${{ inputs.retention_days }}" -gt 7 ]; then
            echo "::error::retention_days は 1〜7 の範囲で指定してください（入力値: ${{ inputs.retention_days }}）"
            exit 1
          fi

  generate-summary-report:
    needs: [validate-inputs]
    if: >-
      inputs.report_types == 'all' || inputs.report_types == 'summary'
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.PROJECT_PAT }}
      PROJECT_OWNER: ${{ github.repository_owner }}
      PROJECT_NUMBER: ${{ inputs.project_number }}
      OUTPUT_FORMAT: ${{ inputs.output_format }}
      ITEM_TYPE: ${{ inputs.item_type }}
      ITEM_STATE: ${{ inputs.item_state }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: サマリーレポート生成
        run: |
          chmod +x scripts/generate-summary-report.sh
          bash scripts/generate-summary-report.sh

      - name: アーティファクトをアップロード
        if: always()
        uses: actions/upload-artifact@v7.0.0
        with:
          name: summary-report-${{ inputs.project_number }}
          path: report-*-summary.*
          if-no-files-found: ignore
          retention-days: ${{ inputs.retention_days }}

  generate-effort-report:
    needs: [validate-inputs]
    if: >-
      inputs.report_types == 'all' || inputs.report_types == 'effort'
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.PROJECT_PAT }}
      PROJECT_OWNER: ${{ github.repository_owner }}
      PROJECT_NUMBER: ${{ inputs.project_number }}
      OUTPUT_FORMAT: ${{ inputs.output_format }}
      ITEM_TYPE: ${{ inputs.item_type }}
      ITEM_STATE: ${{ inputs.item_state }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: 工数集計レポート生成
        run: |
          chmod +x scripts/generate-effort-report.sh
          bash scripts/generate-effort-report.sh

      - name: アーティファクトをアップロード
        if: always()
        uses: actions/upload-artifact@v7.0.0
        with:
          name: effort-report-${{ inputs.project_number }}
          path: report-*-effort.*
          if-no-files-found: ignore
          retention-days: ${{ inputs.retention_days }}

  detect-stale-items:
    needs: [validate-inputs]
    if: >-
      inputs.report_types == 'all' || inputs.report_types == 'stale'
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.PROJECT_PAT }}
      PROJECT_OWNER: ${{ github.repository_owner }}
      PROJECT_NUMBER: ${{ inputs.project_number }}
      OUTPUT_FORMAT: ${{ inputs.output_format }}
      ITEM_TYPE: ${{ inputs.item_type }}
      ITEM_STATE: ${{ inputs.item_state }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: 滞留アイテム検知
        run: |
          chmod +x scripts/detect-stale-items.sh
          bash scripts/detect-stale-items.sh

      - name: アーティファクトをアップロード
        if: always()
        uses: actions/upload-artifact@v7.0.0
        with:
          name: stale-items-report
          path: stale-items-report.*
          if-no-files-found: ignore
          retention-days: ${{ inputs.retention_days }}

  generate-velocity-report:
    needs: [validate-inputs]
    if: >-
      inputs.report_types == 'all' || inputs.report_types == 'velocity'
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.PROJECT_PAT }}
      PROJECT_OWNER: ${{ github.repository_owner }}
      PROJECT_NUMBER: ${{ inputs.project_number }}
      OUTPUT_FORMAT: ${{ inputs.output_format }}
      ITEM_TYPE: ${{ inputs.item_type }}
      ITEM_STATE: ${{ inputs.item_state }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: ベロシティレポート生成
        run: |
          chmod +x scripts/generate-velocity-report.sh
          bash scripts/generate-velocity-report.sh

      - name: アーティファクトをアップロード
        if: always()
        uses: actions/upload-artifact@v7.0.0
        with:
          name: velocity-report-${{ inputs.project_number }}
          path: report-*-velocity.*
          if-no-files-found: ignore
          retention-days: ${{ inputs.retention_days }}

  export-items:
    needs: [validate-inputs]
    if: >-
      inputs.report_types == 'all' || inputs.report_types == 'export'
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.PROJECT_PAT }}
      PROJECT_OWNER: ${{ github.repository_owner }}
      PROJECT_NUMBER: ${{ inputs.project_number }}
      OUTPUT_FORMAT: ${{ inputs.output_format }}
      ITEM_TYPE: ${{ inputs.item_type }}
      ITEM_STATE: ${{ inputs.item_state }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: Project アイテムをエクスポート
        run: |
          chmod +x scripts/export-project-items.sh
          bash scripts/export-project-items.sh

      - name: アーティファクトをアップロード
        if: always()
        uses: actions/upload-artifact@v7.0.0
        with:
          name: export-items-${{ inputs.project_number }}
          path: export-*
          if-no-files-found: ignore
          retention-days: ${{ inputs.retention_days }}

  workflow-summary-failure:
    needs: [validate-inputs, generate-summary-report, generate-effort-report, generate-velocity-report, detect-stale-items, export-items]
    if: always() && contains(needs.*.result, 'failure')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: 失敗サマリーを出力
        uses: ./.github/actions/workflow-summary
        with:
          status: failure
          project-owner: ${{ github.repository_owner }}
          project-number: ${{ inputs.project_number }}
          job-results: |
            {"generate-summary-report": "${{ needs.generate-summary-report.result }}", "generate-effort-report": "${{ needs.generate-effort-report.result }}", "generate-velocity-report": "${{ needs.generate-velocity-report.result }}", "detect-stale-items": "${{ needs.detect-stale-items.result }}", "export-items": "${{ needs.export-items.result }}"}

  workflow-summary-success:
    needs: [validate-inputs, generate-summary-report, generate-effort-report, generate-velocity-report, detect-stale-items, export-items]
    if: always() && !contains(needs.*.result, 'failure') && !contains(needs.*.result, 'cancelled')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: 成功サマリーを出力
        uses: ./.github/actions/workflow-summary
        with:
          status: success
          project-owner: ${{ github.repository_owner }}
          project-number: ${{ inputs.project_number }}
          job-results: |
            {"generate-summary-report": "${{ needs.generate-summary-report.result }}", "generate-effort-report": "${{ needs.generate-effort-report.result }}", "generate-velocity-report": "${{ needs.generate-velocity-report.result }}", "detect-stale-items": "${{ needs.detect-stale-items.result }}", "export-items": "${{ needs.export-items.result }}"}
